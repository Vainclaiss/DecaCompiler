// Description:
//    Test d'une longue expression booléenne avec négation
// Resultats:
//    ok

{
    boolean a = true;
    boolean b = false;
    boolean c = true;
    boolean result = !!!!a && !!b || !!!!c && !(a && !b || c && !a);
    // equivalent : (!!!!a && !!b) || (!!!!c && !((a && !b) || (c && !a) ))
    if(!result){
        println("ok");
    } else {
        println("ERRONE");
    }

}